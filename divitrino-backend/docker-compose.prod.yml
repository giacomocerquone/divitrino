version: "3.7"
services:
  postgres:
    volumes:
      - ./postgres-data:/var/lib/postgresql/data

  rest_api:
    command: bash -c "npm run db:push && ./node_modules/.bin/pm2-runtime ./dist/index.js"
    image: giacomocerquone/divitrino
