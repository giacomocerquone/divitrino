import { IonTitle as ITitle, IonToolbar as IToolbar } from "@ionic/react";
import styled from "styled-components";

export const IonToolbar = styled(IToolbar)`
  --border-width: 0 !important;
`;

export const IonTitle = styled(ITitle)`
  font-weight: normal;
`;
